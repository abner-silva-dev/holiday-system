import Login from '../features/authentication/Login';
import Swipper from '../features/authentication/Swipper';

const Authentication = () => {
  return (
    <>
      <Login />
      <Swipper />
    </>
  );
};

export default Authentication;
