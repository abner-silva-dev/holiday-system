export interface HolidayInfo {
  startingDate: string;
  endDate: string;
  authorizationAdmin: boolean;
  authorizationMannager: boolean;
  observation: string;
  mannagerUser: string;
  adminUser: string;
}
